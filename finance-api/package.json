{
	"name": "finance-api",
	"private": true,
	"type": "module",
	"scripts": {
		"build-worker": "worker-build --dev --panic-unwind",
		"test": "worker-build --dev --panic-unwind && vitest run --config test/vitest.config.ts"
	},
	"devDependencies": {
		"jose": "^6.2.3",
		"miniflare": "^4.20260710.0",
		"typescript": "^6.0.3",
		"vitest": "^4.1.10",
		"wrangler": "^4.107.1"
	}
}
